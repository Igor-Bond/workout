/**
 * Экран выполнения (§12 ТЗ).
 *
 * Главный экран приложения. Появляется на этапе 4, опирается на движок
 * тренировки (этап 3) и на рекорды (§15).
 */

import { ui } from '../core/ui.js';

export const session = {

    title: 'Выполнение',
    nav: 'workout',

    render() {
        return ui.html`
            ${ui.raw(ui.title('Выполнение'))}
            ${ui.raw(ui.stub(
                'Запись подходов',
                4,
                'Поля по виду упражнения, кнопка «Выполнено», прошлый и лучший результат, '
                + 'таймер отдыха, свободный выбор следующего упражнения.'
            ))}
            <button class="btn btn-ghost" data-action="nav" data-screen="home">← На главную</button>
        `;
    }
};
