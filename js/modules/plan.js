/**
 * Составление плана тренировки (§10 ТЗ).
 *
 * Появляется на этапе 3 вместе с движком: экран опирается на справочник
 * упражнений и на прошлые результаты, а те требуют базы.
 */

import { ui } from '../core/ui.js';

export const plan = {

    title: 'План',
    nav: 'workout',

    render() {
        return ui.html`
            ${ui.raw(ui.title('План тренировки'))}
            ${ui.raw(ui.stub(
                'Составление плана',
                3,
                'Выбор упражнений из справочника, количество подходов, целевые повторения '
                + 'и исходный вес, порядок упражнений, сохранение как шаблон.'
            ))}
            <button class="btn btn-ghost" data-action="nav" data-screen="home">← На главную</button>
        `;
    }
};
