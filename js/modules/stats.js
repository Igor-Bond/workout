/**
 * Статистика (§23–§27 ТЗ).
 *
 * Появляется на этапе 6, когда накопятся данные и заработают расчёты.
 */

import { ui } from '../core/ui.js';

export const stats = {

    title: 'Статистика',
    nav: 'stats',

    render() {
        return ui.html`
            ${ui.raw(ui.title('Статистика'))}

            ${ui.raw(ui.stub(
                'Общие показатели',
                6,
                'Тренировки, подходы, повторения, время и тоннаж за выбранный период.'
            ))}

            ${ui.raw(ui.stub(
                'Динамика и частота',
                6,
                'Графики по упражнениям, распределение по дням недели, серия тренировочных дней.'
            ))}
        `;
    }
};
