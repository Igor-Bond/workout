/**
 * Версия приложения — одно число на весь проект (§43 ТЗ).
 *
 * Показывается в профиле. Правится при каждом изменении, которое доезжает
 * до пользователя: мелкая правка — третье число, заметная функция — второе,
 * первое меняется вместе с самим продуктом.
 *
 * Не путать с APP_VERSION в sw.js: та отвечает за сброс кэша и поднимается
 * при любом изменении файлов, даже невидимом.
 */
export const VERSION = '2.21.0';
