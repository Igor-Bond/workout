/**
 * Готовые ответы анкеты (§61).
 *
 * Цель, инвентарь и ограничения человек может написать своими словами — поля
 * для этого есть, — но чаще ответ один из нескольких, и нажать быстрее, чем
 * набрать. Особенно в первый день, особенно на телефоне.
 *
 * Значения по-русски намеренно: они ложатся в профиль как есть и уходят
 * тренеру строкой (§55). Переводятся при показе, как названия базовых
 * упражнений (§53), — и по той же причине: это данные человека, а не подписи
 * интерфейса. Переименовать «резинку» задним числом из-за смены языка
 * приложение не вправе.
 */

/** Зачем человек тренируется. Своё дописывается рядом, в поле. */
export const ЦЕЛИ = ['набрать силу', 'убрать живот', 'держать форму', 'выносливость'];

/** Чем располагает. Без снаряда программа получится не для него. */
export const ИНВЕНТАРЬ = ['резинка', 'гантели', 'штанга', 'турник', 'брусья', 'гиря', 'скакалка'];

/** Что мешает. Названное здесь связывается с упражнениями в профиле (§58). */
export const ОГРАНИЧЕНИЯ = ['колено', 'плечо', 'спина', 'локоть', 'запястье'];
